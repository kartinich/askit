<%= form_with model: @question do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title, requered: true, minlength: 2, placeholder: 'Title' %>
  </div>

  <div>
    <%= f.label :body %>
    <%= f.text_area :body, requered: true, minlength: 2 %>
  </div>

  <%= f.submit "Submit question!" %>
<% end %>
